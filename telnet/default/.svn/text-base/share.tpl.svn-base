^[blue bold]Share^[] (^[bold white]$page_container_size^[], P:^[bold white]$page_number/$page_total^[])
#foreach type name path size requests hits

^[cyan]$name^[] (^[bold white]
#if $size = 0 B
not scanned yet
#else
$size
#endif
^[], R:^[bold white]$requests^[] H:^[bold white]$hits^[])
#next
